# burnout_api - Ortam Değişkenleri Örnek Dosyası
# Bu dosyayı kopyalayıp .env adıyla kaydedin:
#   cp .env.example .env

# Flask ortam ayarları
FLASK_ENV=production
FLASK_DEBUG=False

# İzin verilen CORS origins (React Native dev server)
# CORS_ORIGINS=*
